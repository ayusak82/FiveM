Config = {}

-- ====================================
-- 基本設定
-- ====================================
Config.Debug = true -- デバッグモード（true = コマンドで使用可能、アイテム不要）

-- ====================================
-- アイテム設定
-- ====================================
Config.ItemName = 'jetpack' -- アイテム名（ox_inventoryに登録するアイテム名）

-- ====================================
-- ジェットパック性能設定
-- ====================================
Config.MaxFuel = 100 -- 最大燃料（%）
Config.FuelConsumption = 0.5 -- 燃料消費速度（1秒あたり%）
Config.RefuelAmount = 25 -- 1回の給油で回復する燃料量（%）

Config.Speed = {
    Forward = 15.0, -- 前進速度
    Backward = 10.0, -- 後退速度
    Up = 12.0, -- 上昇速度
    Down = 8.0, -- 下降速度
}

Config.MaxHeight = 200.0 -- 最大高度制限（メートル）

-- ====================================
-- キーバインド設定
-- ====================================
Config.Keys = {
    Activate = 38, -- E key - ジェットパック装着/解除
    MoveUp = 32, -- W key - 上昇
    MoveDown = 33, -- S key - 下降
    MoveForward = 32, -- W key - 前進
    MoveBackward = 33, -- S key - 後退
    TurnLeft = 34, -- A key - 左旋回
    TurnRight = 35, -- D key - 右旋回
}

-- ====================================
-- アニメーション設定
-- ====================================
Config.Animation = {
    Dict = 'move_m@injured', -- アニメーション辞書
    Name = 'sprint', -- アニメーション名
}

-- ====================================
-- エフェクト設定
-- ====================================
Config.Effects = {
    EnableSound = true, -- 効果音ON/OFF
    EnableParticles = true, -- パーティクルエフェクトON/OFF
}

-- ====================================
-- 通知設定
-- ====================================
Config.Notifications = {
    NoFuel = '燃料が切れました！',
    Equipped = 'ジェットパックを装着しました',
    Unequipped = 'ジェットパックを解除しました',
    AlreadyEquipped = 'すでにジェットパックを装着しています',
    MaxHeight = '最大高度に達しました',
    Refueled = '燃料を補給しました',
}

-- ====================================
-- デバッグコマンド設定
-- ====================================
Config.DebugCommand = 'jetpack' -- デバッグモード時のコマンド名